[build]
  base = "client"
  publish = "dist"
  command = "npm run build"

[[redirects]]
  from = "/api/*"
  to = "https://daily-fuel-tracker-web.onrender.com/api/:splat"
  status = 200
  force = true
